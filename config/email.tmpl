Dear {recipient},

This is the comparison of the two most recent captures for {captured_url} ({monitor_uuid}).

Permanent URL of the comparison:
  * https://{domain}/changes_tracking/{monitor_uuid}

Differences between the last two captures:
{details_differences}

The attachment contains the full export.

Best regards,
{sender}
