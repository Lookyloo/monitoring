[tool.poetry]
name = "lookyloo-monitoring"
version = "0.1.0"
description = ""
authors = ["Raphaël Vinot <raphael@vinot.info>"]

[tool.poetry.dependencies]
python = "^3.8"
pylookyloo = "^1.14.2"
plotly = "^5.10.0"
jupyter-dash = "^0.4.2"
jupyterlab = "^3.4.5"
pandas = "^1.4.3"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
run_captures = "bin.run_captures:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
